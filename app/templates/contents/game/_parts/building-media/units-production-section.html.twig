<section class="building-units-production-section">
    <h4>{{ 'Units production' | trans }}</h4>
    <ul class="list-group">
        {% for unit in building.getUnitsProduction(level) %}
            <li class="list-group-item">
                {{ unit | humanize | trans }}
                {# To-Do: Cost! #}
            </li>
        {% endfor %}
    </ul>
</section> <!-- /.building-units-production-section -->
