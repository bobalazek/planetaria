{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Countries' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Country' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.countries.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ country }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.countries') }}">
                {{ 'Countries' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/countries/_form.html.twig' %}
{% endblock %}
