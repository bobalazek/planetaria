{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Towns' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Town' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.towns.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ town }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.towns') }}">
                {{ 'Towns' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/towns/_form.html.twig' %}
{% endblock %}
