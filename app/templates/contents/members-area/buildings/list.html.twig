{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Buildings' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Buildings' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.buildings.new') }}">
            {{ 'New' | trans }}
        </a>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Buildings' | trans }}
        </li>
    </ol>
    {{ paginator_top_render(pagination) }}
    <div class="table-responsive">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th width="100"></th>
                    <th>{{ paginator_sortable(pagination, 'Name', 'b.name') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Slug', 'b.slug') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Description', 'b.description') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Type', 'b.type') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Size', 'b.size') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Health Points', 'b.healthPoints') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Population Capacity', 'b.populationCapacity') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Storage Capacity', 'b.storageCapacity') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Build time', 'b.buildTime') }}</th>
                    <th width="160">{{ paginator_sortable(pagination, 'Time Created', 'b.timeCreated') }}</th>
                </tr>
            </thead>
            <tbody>
                {% for building in pagination.getItems() %}
                    <tr>
                        <td>
                            <div class="object-checkbox-wrapper">
                                <input class="object-checkbox"
                                    type="checkbox"
                                    name="ids[]"
                                    value="{{ building.getId() }}" />
                            </div>
                            <a class="btn btn-xs btn-default"
                                data-toggle="tooltip"
                                title="{{ 'Edit' | trans }}"
                                href="{{ url('members-area.buildings.edit', { id: building.getId() }) }}">
                                <i class="fa fa-fw fa-cog"></i>
                            </a>
                            <a class="btn btn-xs btn-danger"
                                data-toggle="tooltip"
                                title="{{ 'Remove' | trans }}"
                                href="{{ url('members-area.buildings.remove', { id: building.getId() }) }}">
                                <i class="fa fa-fw fa-times"></i>
                            </a>
                        </td>
                        <td>{{ building.getName() }}</td>
                        <td>{{ building.getSlug() }}</td>
                        <td>{{ building.getDescription() }}</td>
                        <td>{{ building.getType() }}</td>
                        <td>{{ building.getSize() }}</td>
                        <td>{{ building.getHealthPoints() }}</td>
                        <td>{{ building.getPopulationCapacity() }}</td>
                        <td>{{ building.getStorageCapacity() }}</td>
                        <td>
                            {{ building.getBuildTime() }}
                            {{ 'seconds' | trans }}
                        </td>
                        <td>{{ building.getTimeCreated() | date(app.dateTimeFormat) }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="9">
                            {{ 'No buildings yet' | trans }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <p>
        <label>
            <input id="check-all-checkbox"
                type="checkbox" />
            {{ 'Check All' | trans }}
        </label>
        -
        {{ 'With Selected' | trans }}:
        <a id="remove-selected-button"
            class="btn btn-xs btn-danger"
            data-toggle="tooltip"
            title="{{ 'Remove' | trans }}"
            href="{{ url('members-area.buildings.remove', { id: 0 }) }}">
            <i class="fa fa-fw fa-times"></i>
        </a>
    </p>
    {{ paginator_bottom_render(pagination) }}
{% endblock %}
