{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Remove' | trans }} | {{ 'Buildings' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {% if buildings %}
            {{ 'Remove Buildings' | trans }}
        {% else %}
            {{ 'Remove Building' | trans }}
            <small>"{{ building }}"</small>
        {% endif %}
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.buildings') }}">
                {{ 'Buildings' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Remove' | trans }}
        </li>
    </ol>
    <div class="alert alert-block alert-warning">
        <h4 class="alert-heading">
            {{ 'Warning!' | trans }}
        </h4>
        {% if buildings %}
            <p>{{ 'Are you sure you want to remove this buildings?' | trans }}</p>
            <ul style="margin-bottom: 10px;">
                {% for building in buildings %}
                    <li>{{ building }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ 'Are you sure you want to remove this building?' | trans }}</p>
        {% endif %}
        <p>
            {% if buildings %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.buildings.remove', { id: 0, action: 'confirm', ids: ids }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% else %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.buildings.remove', { id: building.getId(), action: 'confirm' }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% endif %}
            <a class="btn btn-success"
                href="{{ url('members-area.buildings') }}">
                {{ 'Cancel' | trans }}
            </a>
        </p>
    </div>
{% endblock %}
