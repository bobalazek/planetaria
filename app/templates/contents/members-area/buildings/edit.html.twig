{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Buildings' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Building' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.buildings.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ building }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.buildings') }}">
                {{ 'Buildings' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/buildings/_form.html.twig' %}
{% endblock %}
