{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Skills' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>{{ 'Skills' | trans }}</h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Skills' | trans }}
        </li>
    </ol>
    <div class="table-responsive">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th>{{ 'Key' | trans }}</th>
                    <th>{{ 'Name' | trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for skillKey, skill in skills %}
                    <tr>
                        <td>{{ skillKey }}</td>
                        <td>{{ skill }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
