{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Remove' | trans }} | {{ 'Planets' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {% if planets %}
            {{ 'Remove Planets' | trans }}
        {% else %}
            {{ 'Remove Planets' | trans }}
            <small>"{{ planet }}"</small>
        {% endif %}
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.planets') }}">
                {{ 'Planets' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Remove' | trans }}
        </li>
    </ol>
    <div class="alert alert-block alert-warning">
        <h4 class="alert-heading">
            {{ 'Warning!' | trans }}
        </h4>
        {% if planets %}
            <p>{{ 'Are you sure you want to remove this planets?' | trans }}</p>
            <ul style="margin-bottom: 10px;">
                {% for planet in planets %}
                    <li>{{ planet }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ 'Are you sure you want to remove this planet?' | trans }}</p>
        {% endif %}
        <p>
            {% if planets %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.planets.remove', { id: 0, action: 'confirm', ids: ids }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% else %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.planets.remove', { id: planet.getId(), action: 'confirm' }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% endif %}
            <a class="btn btn-success"
                href="{{ url('members-area.planets') }}">
                {{ 'Cancel' | trans }}
            </a>
        </p>
    </div>
{% endblock %}
