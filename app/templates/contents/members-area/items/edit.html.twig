{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Items' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Item' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.items.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ item }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.items') }}">
                {{ 'Items' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/items/_form.html.twig' %}
{% endblock %}
