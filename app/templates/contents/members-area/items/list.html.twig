{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Items' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Items' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.items.new') }}">
            {{ 'New' | trans }}
        </a>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Items' | trans }}
        </li>
    </ol>
    {{ paginator_top_render(pagination) }}
    <div class="table-responsive">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th width="100"></th>
                    <th>{{ paginator_sortable(pagination, 'Name', 'i.name') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Slug', 'i.slug') }}</th>
                    <th>{{ paginator_sortable(pagination, 'Description', 'i.description') }}</th>
                    <th width="160">{{ paginator_sortable(pagination, 'Time Created', 'i.timeCreated') }}</th>
                </tr>
            </thead>
            <tbody>
                {% for item in pagination.getItems() %}
                    <tr>
                        <td>
                            <div class="object-checkbox-wrapper">
                                <input class="object-checkbox"
                                    type="checkbox"
                                    name="ids[]"
                                    value="{{ item.getId() }}" />
                            </div>
                            <a class="btn btn-xs btn-default"
                                data-toggle="tooltip"
                                title="{{ 'Edit' | trans }}"
                                href="{{ url('members-area.items.edit', { id: item.getId() }) }}">
                                <i class="fa fa-fw fa-cog"></i>
                            </a>
                            <a class="btn btn-xs btn-danger"
                                data-toggle="tooltip"
                                title="{{ 'Remove' | trans }}"
                                href="{{ url('members-area.items.remove', { id: item.getId() }) }}">
                                <i class="fa fa-fw fa-times"></i>
                            </a>
                        </td>
                        <td>{{ item.getName() }}</td>
                        <td>{{ item.getSlug() }}</td>
                        <td>{{ item.getDescription() }}</td>
                        <td>{{ item.getTimeCreated() | date(app.dateTimeFormat) }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="9">
                            {{ 'No items yet' | trans }}
                        </td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
    <p>
        <label>
            <input id="check-all-checkbox"
                type="checkbox" />
            {{ 'Check All' | trans }}
        </label>
        -
        {{ 'With Selected' | trans }}:
        <a id="remove-selected-button"
            class="btn btn-xs btn-danger"
            data-toggle="tooltip"
            title="{{ 'Remove' | trans }}"
            href="{{ url('members-area.items.remove', { id: 0 }) }}">
            <i class="fa fa-fw fa-times"></i>
        </a>
    </p>
    {{ paginator_bottom_render(pagination) }}
{% endblock %}
