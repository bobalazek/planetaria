{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Resources' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Resource' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.resources.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ resource }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.resources') }}">
                {{ 'Resources' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/resources/_form.html.twig' %}
{% endblock %}
