{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Resources' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>{{ 'Resources' | trans }}</h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Resources' | trans }}
        </li>
    </ol>
    <div class="table-responsive">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th>{{ 'Key' | trans }}</th>
                    <th>{{ 'Name' | trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for resourceKey, resource in resources %}
                    <tr>
                        <td>{{ resourceKey }}</td>
                        <td>{{ resource }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
