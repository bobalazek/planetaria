{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Remove' | trans }} | {{ 'Units' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {% if units %}
            {{ 'Remove Units' | trans }}
        {% else %}
            {{ 'Remove Units' | trans }}
            <small>"{{ unit }}"</small>
        {% endif %}
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.units') }}">
                {{ 'Units' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Remove' | trans }}
        </li>
    </ol>
    <div class="alert alert-block alert-warning">
        <h4 class="alert-heading">
            {{ 'Warning!' | trans }}
        </h4>
        {% if units %}
            <p>{{ 'Are you sure you want to remove this units?' | trans }}</p>
            <ul style="margin-bottom: 10px;">
                {% for unit in units %}
                    <li>{{ unit }}</li>
                {% endfor %}
            </ul>
        {% else %}
            <p>{{ 'Are you sure you want to remove this unit?' | trans }}</p>
        {% endif %}
        <p>
            {% if units %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.units.remove', { id: 0, action: 'confirm', ids: ids }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% else %}
                <a class="btn btn-danger"
                    href="{{ url('members-area.units.remove', { id: unit.getId(), action: 'confirm' }) }}">
                    {{ 'Confirm' | trans }}
                </a>
            {% endif %}
            <a class="btn btn-success"
                href="{{ url('members-area.units') }}">
                {{ 'Cancel' | trans }}
            </a>
        </p>
    </div>
{% endblock %}
