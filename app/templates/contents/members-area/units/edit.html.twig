{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Edit' | trans }} | {{ 'Units' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>
        {{ 'Edit Unit' | trans }}
        <a class="btn btn-primary btn-sm"
            href="{{ url('members-area.units.new') }}">
            {{ 'New' | trans }}
        </a>
        <br />
        <small>{{ unit }}</small>
    </h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li>
            <a href="{{ url('members-area.units') }}">
                {{ 'Units' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Edit' | trans }}
        </li>
    </ol>
    {% include 'contents/members-area/units/_form.html.twig' %}
{% endblock %}
