{% extends 'layouts/content/members-area.html.twig' %}

{% block title %}{{ 'Units' | trans }} | {{ parent() }}{% endblock %}

{% block main %}
    <h1>{{ 'Units' | trans }}</h1>
    <ol class="breadcrumb">
        <li>
            <a href="{{ url('members-area') }}">
                {{ 'Home' | trans }}
            </a>
        </li>
        <li class="active">
            {{ 'Units' | trans }}
        </li>
    </ol>
    <div class="table-responsive">
        <table class="table table-striped table-condensed">
            <thead>
                <tr>
                    <th>{{ 'Key' | trans }}</th>
                    <th>{{ 'Name' | trans }}</th>
                </tr>
            </thead>
            <tbody>
                {% for unitKey, unit in units %}
                    <tr>
                        <td>{{ unitKey }}</td>
                        <td>{{ unit }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
