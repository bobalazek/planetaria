{% extends 'layouts/email.html.twig' %}

{% block main %}
    <table class="row note">
        <tr>
            <td class="wrapper last">
                <h4>
                    {{ 'Hello' | trans }}
                    <b>{{ user.getUsername() }}</b>!
                </h4>
                <p>{{ 'Your password has been successfully changed!' | trans }}</p>
            </td>
        </tr>
    </table>
{% endblock %}
